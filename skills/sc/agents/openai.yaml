interface:
  display_name: "SI-Coder"
  short_description: "Build and publish web apps from plain-language goals"
  default_prompt: "Use $sc to turn my idea into a working web app, choose sensible defaults, and publish it when ready."
policy:
  allow_implicit_invocation: true
